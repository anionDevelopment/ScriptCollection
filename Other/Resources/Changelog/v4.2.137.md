# Release notes

## Changes

- fixed secret-scanning in the build-pipeline producing false positives because betterleaks scanned the wrong directory when running inside the build-container (now the repository is shared with the sibling betterleaks-container via its volumes instead of an unresolvable host bind-mount)
- reworked the betterleaks-config to allowlist the example-/test-certificates on top of the default ruleset instead of replacing the ruleset
