# Release notes

## Changes

- fix
- the generation of the reference of a dotnet-codeunit does not leave a lock-file without a runtime in its name behind
  anymore: docfx is called with "--noRestore" and what its metadata-step reads is restored before it, because the
  restore which docfx runs on its own is a process of its own which no property of the command-line reaches
