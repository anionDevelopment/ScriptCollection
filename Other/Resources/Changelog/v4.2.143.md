# Release notes

## Changes

- Added `scremovetrailinglinebreak` command and `remove_trailing_linebreak` in `ScriptCollectionCore`
- Moved `get_scriptcollection_configuration_folder` to `GeneralUtilities` as a static method; `ScriptCollectionCore` now delegates to it
