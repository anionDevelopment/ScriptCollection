# Release notes

## Changes

- added scruncommandinfolder
- fixed scdownloadcachabletools failing when warming the global cache because no OpenAPIGenerator-version was pinned (now falls back to a default version when there is no repository-context)
