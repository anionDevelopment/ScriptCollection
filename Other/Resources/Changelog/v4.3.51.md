# Release notes

## Changes

- update for plantuml cache
- The secret-scan (search_for_secrets) now checks whether the docker-daemon is reachable before running betterleaks and raises a clear infrastructure-error when it is not, instead of misreporting an unreachable daemon as "found secrets"
