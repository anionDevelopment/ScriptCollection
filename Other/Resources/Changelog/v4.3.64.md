# Release notes

## Changes

- fixed initialization-inconsistency
- added ScriptCollectionCore.docker_pull, ScriptCollectionCore.docker_compose_pull, ScriptCollectionCore.local_docker_image_exists and ScriptCollectionCore.split_image_address_and_tag
- images which are required by a docker-command are now pulled explicitly instead of being downloaded implicitly, so the progress-output of the download is not logged as error anymore
