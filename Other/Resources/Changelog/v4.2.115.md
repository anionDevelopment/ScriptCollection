# Release notes

## Changes

- Fixed start_local_test_service so the build-container attaches itself to the (external) compose-network of the test-services. This makes the sibling test-service-containers routable and resolvable from inside the build-container (docker-socket-mounted runners), instead of only resolving the container-name to an unreachable IP which resulted in connection-timeouts.
